export type LoginTypes = {
    email: string,
    password: string,
};

export type RegistTypes = {
    fullname: string
    contact: number
    address: string
    email: string
    password: string
}   